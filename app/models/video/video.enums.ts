enum VideoOrigin {
    Unknown = 0,
    YouTube = 1,
    Vimeo,
    Channel9,
    
}

export {VideoOrigin};

//let type = VideoType.YouTube
//let videoTypeString = VideoType[type]; // displays YouTube